# 2026-09-17 — Long-term memory (append-only)

## Prompts

- "rode a skill orchestrator" — full orchestrator run, Phase -1→7.
- "atualize a skill deste repo com as novas skills atualizadas do
  afonsoft/skills" — skills catalog sync.
- "depois rode a skill orchestrator e a gap-analysis" — full audit.
- "aprovadas" — approved 5 gap-analysis Draft SPECs.
- "contine 'Provisionar tudo'" — harness-files option (a) chosen.

## Decisions

- Adopted skills.sh canonical layout: `.agents/skills/` real files
  (gitignored), `.claude/skills/*` symlinks, `skills-lock.json` SHA-256 pins.
- Gap-analysis 20260917 produced 5 SPECs; all approved, Epic #90 + issues
  #91–#95 created.
- Branch protection applied to `main` with `enforce_admins=false` — owner
  retains emergency bypass, documented in CLAUDE.md.
- Harness-files: user chose option (a) — full `.claude/` harness provisioned.

## Checkpoints

- PRs #87 (format gate fix + SPEC sync), #88 (skills update), #89 (SPECs+report),
  #96 (README sync) merged; main at `00f1c18` green.
- Merged branches cleaned: 13 local + 16 remote deleted; 2 unmerged kept.
- Format gate restored: `dotnet format --verify-no-changes` exit 0.

## Lessons learned

- `dotnet format` has no `--nologo` flag — use `--verify-no-changes` directly.
- `gh pr merge --auto` requires repo auto-merge setting — not enabled here;
  merge manually after checks go green.
- `enforce_admins=false` means owner pushes are NOT blocked by protection —
  verify the gate via API config, not a test push (would succeed for admin).

## Session summary

Epic #90 execution: S1 merges done, S3 branch cleanup done, S5 README sync
merged (#96), S2 branch protection applied + doc PR #97, S4 harness-files
option (a) provisioned on `chore/Devin-20260917-harness-files`. Pending:
merge #97 + harness PR, close #92/#94, Phase 8 persistence.
