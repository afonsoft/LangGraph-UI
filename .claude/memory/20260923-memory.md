# Long-term memory — 2026-09-23

## Prompts

- User asked to read/analyze the repo, analyze an external proposal ("Guia Prático: RAG e GraphRAG com C#, MCP, Ollama e Bancos Vetoriais"), identify gaps, create detailed SPECs for each improvement (performance, memory, response time, cache, etc.), then commit, push and merge to main.

## Decisions

- Ran gap-analysis skill against the proposal as TO-BE source; verdicts and evidence in `.claude/memory/gap-analysis-20260923.md`.
- 13 candidates → 13 CONFIRMADO → consolidated into 10 Draft SPECs (grouped by cohesion; GraphRAG marked phase-4/conditional per the proposal's own sequencing).
- SPECs committed as `Status: Draft` — implementation requires per-SPEC approval via execute-specs; user deliverable was the spec set merged to main.
- Branch `feature/Devin-20260923-rag-gap-specs` per naming convention; docs-only change → PR + merge to main.

## Findings (evidence-backed)

- `PostgresVectorStore.EnsureInitializedAsync` (:99-109) creates only a `model` btree — no HNSW → seq scan at scale; `UpsertBatchAsync` not overridden → N INSERT round-trips.
- `AgentService.StreamAsync` (:54) `Channel.CreateUnbounded<SseEvent>` — memory growth under slow clients (H7 deferred from SPEC-20260916).
- `AnswerService.BuildUserPrompt` (:129-143) injects chunk text verbatim — no delimiters/detection (prompt-injection surface).
- `SearchResultItem` lacks ChunkId/DocumentId/Metadata/IndexedAt; tools expose only `source`/`mode` filters.
- No `Meter`/`ActivitySource`/OTel, no `RateLimiter`, no reranker, no query rewriting, no eval harness, no per-key source scoping, no code-aware chunker, no graph layer.
- Already-covered proposal items (not gaps): sqlite-vec opt-in, pgvector opt-in, FTS5+RRF, IDistributedCache memory|redis, batch embed/upsert, DbContextPool, [n] citations, HITL, per-key integration secrets.

## Session summary

Delivered 10 Draft SPECs + gap-analysis report on `feature/Devin-20260923-rag-gap-specs`; PR → merged to main per user instruction. Work stops here; implementation awaits SPEC approval.
